---
phase: 02-refactoring
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - models/users.py
  - models/servers.py
  - models/schedules.py
  - models/jobs.py
  - models/settings.py
  - models/ansible.py
  - app.py
autonomous: true
requirements: [REF-02]

must_haves:
  truths:
    - "Every SQL query in the codebase lives inside a models/*.py file"
    - "app.py has zero conn.execute() calls and zero get_db() calls"
    - "All existing routes return the same data as before (no regressions)"
  artifacts:
    - path: "models/users.py"
      provides: "Repository for users table"
      contains: "def get_by_username"
    - path: "models/servers.py"
      provides: "Repository for servers table"
      contains: "def get_all"
    - path: "models/schedules.py"
      provides: "Repository for schedules table"
      contains: "def get_by_server"
    - path: "models/jobs.py"
      provides: "Repository for backup_jobs table"
      contains: "def get_all"
    - path: "models/settings.py"
      provides: "Repository for settings table"
      contains: "def get_settings"
    - path: "models/ansible.py"
      provides: "Repository for all ansible_* tables"
      contains: "def get_hosts"
  key_links:
    - from: "app.py"
      to: "models/servers.py"
      via: "from models.servers import ..."
      pattern: "from models\\.servers import"
    - from: "app.py"
      to: "models/users.py"
      via: "from models.users import ..."
      pattern: "from models\\.users import"
    - from: "app.py"
      to: "models/ansible.py"
      via: "from models.ansible import ..."
      pattern: "from models\\.ansible import"
---

<objective>
Extract all ~260 raw SQL conn.execute() calls from app.py into typed repository functions in models/*.py modules. After this plan, app.py has zero direct DB access.

Purpose: Satisfy REF-02 (no inline SQL in routes or services). Creates the data access layer that services and routes will import in later plans.
Output: 6 repository modules in models/, app.py updated to call repository functions
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-refactoring/02-RESEARCH.md
@.planning/phases/02-refactoring/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repository modules for users, servers, schedules, jobs, and settings</name>
  <files>models/users.py, models/servers.py, models/schedules.py, models/jobs.py, models/settings.py, app.py</files>
  <read_first>
    - app.py (full file — needed to identify every conn.execute() call and its context)
    - db.py (to confirm get_db import path)
    - models/__init__.py (confirm exists)
  </read_first>
  <action>
Create repository modules by extracting ALL SQL queries from app.py. Each module imports `from db import get_db` and exposes plain functions (NOT classes).

**models/users.py** — Extract from app.py's user-related SQL:
- `get_by_username(username)` — from `_get_user_by_username()` (line ~670)
- `get_by_id(uid)` — used in user_edit route
- `get_all()` — used in users_list route
- `create(username, password_hash, role)` — from user_add route
- `update(uid, **kwargs)` — from user_edit route (password_hash and/or role)
- `delete(uid)` — from user_delete route
- `update_password(uid, password_hash)` — from change_password route
- `count_admins()` — used in user_delete to prevent deleting last admin

**models/servers.py** — Extract from app.py's server-related SQL:
- `get_all()` — from servers_list route
- `get_by_id(sid)` — from server_detail, server_edit, etc.
- `create(label, hostname, ip_address, ssh_port, ssh_user, ssh_auth, ssh_password, become_method, become_user, become_password, become_same_pass, exclude_dirs, notes)` — from server_add
- `update(sid, **kwargs)` — from server_edit
- `delete(sid)` — from server_delete (also deletes related schedules and jobs)
- `get_server_count()` — from dashboard
- `bulk_create(servers_list)` — from server_bulk_add route

**models/schedules.py** — Extract from app.py's schedule-related SQL:
- `get_by_server(sid)` — used in server_detail
- `get_by_id(scid)` — used in schedule_toggle, schedule_delete, schedule_run_now
- `get_all_enabled()` — used in init_scheduler, _restart_scheduler_with_timezone
- `create(server_id, minute, hour, dom, month, dow, enabled)` — from schedule_add
- `toggle(scid)` — from schedule_toggle
- `delete(scid)` — from schedule_delete
- `get_count()` — from dashboard

**models/jobs.py** — Extract from app.py's backup_jobs-related SQL:
- `get_all(limit=None)` — from jobs_list
- `get_by_id(jid)` — from job_detail
- `get_by_server(sid)` — from server_detail
- `create(server_id, job_type, triggered_by, schedule_id)` — from create_job function
- `update_status(jid, status, extra=None)` — from _set_job_status
- `append_log(jid, text)` — from _append_log
- `delete(jid)` — from job_delete
- `get_running_count()` — from dashboard (count of status='running')
- `get_recent(limit=10)` — from dashboard

**models/settings.py** — Extract from app.py's settings-related SQL:
- `get_settings()` — from get_settings function (line ~593)
- `save_setting(key, value)` — from save_setting function (line ~600)
- `get_nfs_target(hostname)` — from get_nfs_target function (line ~656)

For each function, preserve the exact SQL query strings from app.py. Do NOT rewrite or optimize queries — this is a pure extraction refactor.

Pattern for each function:
```python
def get_all():
    conn = get_db()
    rows = conn.execute('SELECT * FROM servers ORDER BY label').fetchall()
    conn.close()
    return rows
```

After creating all models, update app.py:
- Add imports: `from models import users as user_repo, servers as server_repo, schedules as schedule_repo, jobs as job_repo, settings as settings_repo`
- Replace every inline SQL call in route handlers and helper functions with the corresponding repository function call
- Remove the old `get_settings()`, `save_setting()`, `get_nfs_target()`, `_get_user_by_username()` functions from app.py (they now live in models)
- Keep `_append_log()` and `_set_job_status()` in app.py for now (they move to services in Plan 3) but change their internals to call `job_repo.append_log()` and `job_repo.update_status()`

CRITICAL: Do not move any non-SQL functions. Route handlers, SSH functions, scheduler functions, decorators — all stay in app.py. Only SQL access moves.

After all replacements, verify: `grep -rn "get_db()\|conn\.execute\|conn\.commit\|conn\.close" app.py` should return ZERO results (all DB access is now in models/ or db.py).
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/rear-manager && grep -c "get_db()" app.py && echo "---" && grep -c "conn.execute" app.py && echo "---" && python -c "import app" && python -m pytest tests/ -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_db()" app.py` returns 0
    - `grep -c "conn.execute" app.py` returns 0
    - models/users.py contains `def get_by_username(`
    - models/servers.py contains `def get_all():`
    - models/schedules.py contains `def get_all_enabled():`
    - models/jobs.py contains `def create(`
    - models/settings.py contains `def get_settings():`
    - All model files contain `from db import get_db`
    - `python -c "import app"` exits 0
    - `python -m pytest tests/ -x -q` exits 0 (full suite still passes including smoke tests)
  </acceptance_criteria>
  <done>All ~260 SQL calls extracted to models/ repository modules; app.py has zero direct DB access; all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create ansible repository module and verify layer boundary</name>
  <files>models/ansible.py, app.py</files>
  <read_first>
    - app.py (lines 3109-3430 for Ansible service functions with SQL, lines 3431-4314 for Ansible routes with SQL)
    - models/__init__.py
  </read_first>
  <action>
Create `models/ansible.py` — the largest repository module, covering all 6 ansible_* tables. Extract ALL SQL from Ansible-related functions in app.py.

**models/ansible.py** must contain:

Hosts:
- `get_hosts()` — all ansible hosts with group info
- `get_host_by_id(hid)` — single host
- `create_host(hostname, ip_address, ssh_port, ssh_user, ssh_auth, ssh_password, become_method, become_user, become_password, become_same_pass, python_interpreter, extra_vars)` — from ansible_host_add
- `update_host(hid, **kwargs)` — from _save_ansible_host
- `delete_host(hid)` — from ansible_host_delete
- `get_host_groups(hid)` — group memberships for a host
- `set_host_groups(hid, group_ids)` — replace group memberships
- `bulk_create_hosts(hosts_data)` — from ansible_host_bulk_add
- `link_server_to_host(sid, hid)` — from server_ansible_link
- `unlink_server_host(sid)` — from server_ansible_unlink
- `get_host_by_server(sid)` — find ansible host linked to a server
- `create_host_from_server(server_dict)` — from server_ansible_auto_add

Groups:
- `get_groups()` — all groups with host counts
- `create_group(name, description)` — from ansible_groups route
- `delete_group(gid)` — from ansible_groups route

Playbooks:
- `get_playbooks()` — all playbooks
- `get_playbook_by_id(pid)` — single playbook
- `create_playbook(name, content, description, extra_args)` — from ansible_playbook_add
- `update_playbook(pid, **kwargs)` — from _save_playbook
- `delete_playbook(pid)` — from ansible_playbook_delete

Runs:
- `get_runs(limit=None)` — all runs with playbook name
- `get_run_by_id(rid)` — single run
- `create_run(playbook_id, playbook_name, inventory_content, extra_args, triggered_by)` — from ansible_playbook_run
- `update_run_status(rid, status, exit_code=None)` — from _set_run_status
- `append_run_log(rid, text)` — from _append_run_log
- `delete_run(rid)` — from ansible_run_delete

Roles:
- `get_roles()` — all roles
- `get_role_by_id(rid)` — single role with files
- `create_role(name, description, role_type)` — from ansible_role_add
- `update_role(rid, **kwargs)` — from ansible_role_save
- `delete_role(rid)` — from ansible_role_delete (also deletes role files and disk directory)
- `get_role_files(rid)` — files belonging to a role
- `create_role_file(role_id, filename, content, file_type)` — from ansible_role_add_file
- `update_role_file(fid, content)` — from ansible_role_save_file
- `delete_role_file(fid)` — if needed

Dashboard stats:
- `get_host_count()` — from ansible_dashboard
- `get_group_count()` — from ansible_dashboard
- `get_playbook_count()` — from ansible_dashboard
- `get_run_count()` — from ansible_dashboard

Update app.py to import `from models import ansible as ansible_repo` and replace all Ansible SQL calls in route handlers and service functions.

After this task, run the full boundary check:
```bash
grep -rn "get_db()\|conn\.execute" app.py  # must return 0 lines
grep -rn "get_db()\|conn\.execute" routes/ services/  # must return 0 lines (dirs are empty)
```
  </action>
  <verify>
    <automated>cd /home/ubuntu/workspace/rear-manager && grep -c "get_db()" app.py && echo "---" && grep -c "conn.execute" app.py && echo "---" && python -c "from models.ansible import get_hosts, get_playbooks, get_runs, get_roles" && python -m pytest tests/ -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_db()" app.py` returns 0
    - `grep -c "conn.execute" app.py` returns 0
    - models/ansible.py contains `def get_hosts(`
    - models/ansible.py contains `def get_playbooks(`
    - models/ansible.py contains `def get_runs(`
    - models/ansible.py contains `def get_roles(`
    - models/ansible.py contains `from db import get_db`
    - `python -c "from models.ansible import get_hosts"` exits 0
    - `python -m pytest tests/ -x -q` exits 0 (full suite passes)
  </acceptance_criteria>
  <done>Ansible repository module complete; app.py has zero SQL calls across the entire file; all tests pass; REF-02 fully satisfied</done>
</task>

</tasks>

<verification>
- `grep -rn "get_db()" app.py` returns 0 lines
- `grep -rn "conn\.execute" app.py` returns 0 lines
- `python -m pytest tests/ -x -q` — full suite passes
- `python -c "from models.users import get_by_username; from models.servers import get_all; from models.ansible import get_hosts"` — all imports work
- Each models/*.py file contains `from db import get_db`
</verification>

<success_criteria>
- All 6 repository modules exist in models/
- Zero conn.execute() or get_db() calls remain in app.py
- All 62 routes return non-500 (smoke test passes)
- Full existing test suite passes
- REF-02 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-refactoring/02-02-SUMMARY.md`
</output>
