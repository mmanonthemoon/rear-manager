---
phase: 02-refactoring
task: complete
total_tasks: 6
status: in_progress
last_updated: "2026-03-25T21:32:40.013Z"
---

<current_state>
Phase 02 (Refactoring) is mid-execution. Plans 02-01 through 02-04 are complete (foundation + all model/service layers extracted). Plans 02-05 (Routes/Blueprints) and 02-06 (Exception cleanup) remain. app.py is currently a hybrid: it still contains all 62 route handlers but already imports from the new models/ and services/ modules.
</current_state>

<completed_work>

- Plan 02-01: Foundation scaffolding — config.py (14 constants), db.py (DB helpers), empty package dirs (models/, services/, routes/), 36 smoke tests; 55 tests passing
- Plan 02-02: Models/repository layer — 6 repo modules (users, servers, schedules, jobs, settings, ansible); app.py has zero direct DB calls
- Plan 02-03: Services part 1 — services/ssh.py (418 lines, typed SSH exceptions), services/rear.py (538 lines), services/jobs.py (188 lines, mutable globals with accessors); 55 tests passing
- Plan 02-04: Services part 2 — services/auth.py (login_required/admin_required), services/scheduler.py (APScheduler management), services/ansible.py (full orchestration); 55 tests passing
</completed_work>

<remaining_work>

- Plan 02-05: Routes layer — move all 62 route handlers from app.py into 9 Flask Blueprint modules (routes/auth.py, dashboard.py, servers.py, schedules.py, jobs.py, settings.py, users.py, api.py, ansible.py); update all url_for() calls in Python + 28 templates; largest plan (37 files)
- Plan 02-06: Exception cleanup — replace remaining bare `except Exception` blocks across routes/, services/, models/ with typed exceptions; background thread catch-alls kept but must have `# broad-catch-ok` + `traceback.format_exc()` logging
</remaining_work>

<decisions_made>

- Hold `_job_lock` only for dict operations, not across DB or render_template calls
- Mutable globals (_running_jobs, _job_lock, _scheduler, _ansible_running, etc.) wrapped in accessor functions owned by respective service modules
- Background threads get explicit app.app_context() push via current_app._get_current_object()
- SSH exception hierarchy: SSHConnectionError > SSHAuthenticationError (typed, not bare except)
- login_required/admin_required decorators live in services/auth.py
- Jinja template filters stay in app.py, registered on app object before Blueprint registration
- Bare except blocks that must remain flagged with `# broad-catch-ok` + traceback logging
</decisions_made>

<blockers>

- None active. Plan 02-05 has 37 files (28 templates + 9 Python) — mechanical find-replace but high file count. Plan-checker rated as warning, not blocker.
</blockers>

<context>
Sequential horizontal-layer refactor of 4,329-line Flask monolith. Each plan depends on the previous. Smoke test suite (test_smoke_routes.py, 36 tests) runs as regression safety net after each plan. All 55 tests currently passing. Remaining 2 plans are well-defined mechanically despite high file count.
</context>

<next_action>
Run `/gsd:execute-phase 2` (after `/clear` for fresh context). The executor will auto-skip completed plans 02-01 through 02-04 (they have SUMMARY.md files) and start from 02-05.
</next_action>
